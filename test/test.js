'use strict';

var radiator = require('../lib/radiator.js');

exports.radiator = {
  setUp: function(done) {
    // setup here
    done();
  },

  'Should warm my frozen soul': function(test) {
    // TODO: Potentially refactor this scientificly perfect test
    test.ok(true);
    test.done();
  },
};
